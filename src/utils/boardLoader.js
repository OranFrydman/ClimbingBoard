/**
 * Re-exports from the board registry (one JSON per board).
 * Use this or import from '../data/boards/boardRegistry' directly.
 */
export { getBoard, fetchBoard, getBoardList, BOARD_ENUM, DEFAULT_BOARD_ID } from '../data/boards/boardRegistry';
